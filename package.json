{
  "name": "waveform",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check audio-injector.js && node --check content.js && node --check background.js && node --check popup/popup.js",
    "fixtures": "node tests/fixtures/server.cjs",
    "package": "node scripts/package.cjs"
  },
  "engines": { "node": ">=22" }
}
